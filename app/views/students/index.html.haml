/* Index */

.container-top.left
  .breadcrumbs
    .breadcrumbs-box
      .breadcrumbs-item
        = link_to root_path, class: "link small" do
          .icon.left
            = image_tag "icons/back.png"
          = t("home")

.container-middle.left

  .content
    .content-box
      .content-item
        - if @object.title.presence
          %h1= @object.try(:title)
        .separator.small
        - if @object.description.presence
          .trix
            = safe_join([@object.try(:description).html_safe])

  .separator.small

  .partnerships
    .partnerships-box
      - @wallet_categories.each do |wallet_category|
        - wallet_category.assets.each do |asset|
          .partnerships-item
            .partnerships-inner
              = link_to asset.document_box_url.to_s, target: :blank, class: "blue" do
                - if asset.document_caption.presence
                  = asset.document_caption
                - else
                  = t("document")
.container-bottom.left
