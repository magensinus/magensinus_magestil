/* Index */

.container-top.left
  / Breadcrumbs
  .breadcrumbs
    .breadcrumbs-box
      .breadcrumbs-item
        = link_to root_path, class: "link small" do
          .icon.left
            = image_tag "icons/back.png"
          = t("home")

.container-middle.left
  / Content
  .content
    .content-box
      .content-item
        - if @tutors.title.presence
          %h1= @tutors.try(:title)
        .separator.small
        - if @tutors.description.presence
          .trix
            = safe_join([@tutors.try(:description).html_safe])

  .separator.small

  = render partial: "wallets"

.container-bottom.left
